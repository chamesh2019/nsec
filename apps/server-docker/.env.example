# Port to expose the NullSec server on the host (default: 4000)
PORT=4000

# Optional: Master bootstrap token for emergency or headless admin registration
# NSEC_BOOTSTRAP_TOKEN=your_secure_bootstrap_token_here
