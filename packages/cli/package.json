{
  "name": "@zvault/cli",
  "version": "0.1.0",
  "description": "Zero-knowledge secret vault CLI for secure process injection and management",
  "license": "MIT",
  "type": "module",
  "bin": {
    "zvault": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "node --import tsx --test 'test/**/*.test.ts'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@zvault/core": "workspace:*",
    "@zvault/crypto": "workspace:*",
    "@zvault/keyring": "workspace:*",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
